# Backend API Base URL
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000/api/v1

# Optional: Admin API Key for protected endpoints (server-side only)
# Do NOT prefix with NEXT_PUBLIC_ as this should not be exposed to client
ADMIN_API_KEY=
